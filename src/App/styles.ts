import styled from 'styled-components';

import { AppShell } from '@mantine/core';

export const AppContainer = styled(AppShell)`
	text-align: center;
	margin: 0 auto;
	max-width: 1180px;
	min-height: 100vh;
	padding: 0 3rem 0 2rem;
`;
